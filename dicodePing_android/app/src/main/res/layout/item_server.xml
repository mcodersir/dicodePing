<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    style="@style/Widget.DicodePing.ServerCard"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="10dp"
    android:clickable="true"
    android:focusable="true"
    android:foreground="?attr/selectableItemBackground">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layoutDirection="locale"
        android:orientation="vertical"
        android:paddingStart="14dp"
        android:paddingTop="13dp"
        android:paddingEnd="14dp"
        android:paddingBottom="11dp">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center_vertical"
            android:layoutDirection="locale"
            android:orientation="horizontal">

            <TextView
                android:id="@+id/serverFlag"
                android:layout_width="48dp"
                android:layout_height="48dp"
                android:background="@drawable/bg_server_number"
                android:gravity="center"
                android:text="🌐"
                android:textSize="23sp" />

            <LinearLayout
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_marginStart="11dp"
                android:layout_marginEnd="10dp"
                android:layout_weight="1"
                android:orientation="vertical">

                <TextView
                    android:id="@+id/serverName"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:ellipsize="end"
                    android:includeFontPadding="false"
                    android:maxLines="1"
                    android:textColor="@color/text_primary"
                    android:textSize="14sp"
                    android:textStyle="bold" />

                <TextView
                    android:id="@+id/location"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="6dp"
                    android:drawablePadding="5dp"
                    android:ellipsize="end"
                    android:includeFontPadding="false"
                    android:maxLines="1"
                    android:textColor="@color/text_secondary"
                    android:textSize="11sp"
                    app:drawableStartCompat="@drawable/ic_location"
                    app:drawableTint="@color/text_tertiary" />

                <TextView
                    android:id="@+id/network"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:visibility="gone" />
            </LinearLayout>

            <TextView
                android:id="@+id/ping"
                android:layout_width="wrap_content"
                android:layout_height="30dp"
                android:background="@drawable/bg_ping_neutral"
                android:gravity="center"
                android:minWidth="88dp"
                android:paddingStart="9dp"
                android:paddingEnd="9dp"
                android:text="—"
                android:textColor="@color/text_secondary"
                android:textDirection="ltr"
                android:textSize="9sp"
                android:textStyle="bold" />
        </LinearLayout>

        <View
            android:id="@+id/itemDivider"
            android:layout_width="match_parent"
            android:layout_height="1dp"
            android:layout_marginTop="12dp"
            android:layout_marginBottom="9dp"
            android:background="@color/outline_subtle" />

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="48dp"
            android:gravity="center_vertical"
            android:layoutDirection="locale"
            android:orientation="horizontal">

            <TextView
                android:id="@+id/selectedBadge"
                android:layout_width="wrap_content"
                android:layout_height="32dp"
                android:background="@drawable/bg_selected_pill"
                android:drawablePadding="5dp"
                android:gravity="center"
                android:paddingStart="10dp"
                android:paddingEnd="10dp"
                android:text="@string/selected_server"
                android:textColor="@color/brand"
                android:textSize="10sp"
                android:textStyle="bold"
                android:visibility="invisible"
                app:drawableStartCompat="@drawable/ic_check" />

            <Space
                android:layout_width="0dp"
                android:layout_height="1dp"
                android:layout_weight="1" />

            <com.google.android.material.button.MaterialButton
                android:id="@+id/favorite"
                style="@style/Widget.DicodePing.IconButton"
                android:layout_width="48dp"
                android:layout_height="48dp"
                android:contentDescription="@string/favorite_server"
                app:icon="@drawable/ic_star_outline"
                app:iconGravity="textStart"
                app:iconPadding="0dp"
                app:iconSize="20dp" />

            <com.google.android.material.button.MaterialButton
                android:id="@+id/connect"
                style="@style/Widget.DicodePing.Button.ServerAction"
                android:layout_width="wrap_content"
                android:layout_height="48dp"
                android:layout_marginStart="8dp"
                android:minWidth="92dp"
                android:text="@string/connect"
                android:textSize="11sp"
                app:icon="@drawable/ic_bolt"
                app:iconGravity="textStart"
                app:iconPadding="6dp"
                app:iconSize="16dp" />
        </LinearLayout>

        <TextView
            android:id="@+id/serverMeta"
            android:layout_width="0dp"
            android:layout_height="0dp"
            android:visibility="gone" />
    </LinearLayout>
</com.google.android.material.card.MaterialCardView>
