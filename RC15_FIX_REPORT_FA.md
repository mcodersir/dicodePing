# گزارش اصلاح RC15

- رفع کرش `Expected URL scheme http or https` در `SubscriptionClient.revision`.
- نرمال‌سازی امن URL و مهاجرت تنظیمات خراب RC14.
- افزایش `versionCode` اندروید به 50.
- اجباری شدن ساخت و بسته‌بندی Aether و Usque در خود `build_apk.sh`.
- بررسی وجود و معماری `libgojni.so`، `libaether.so` و `libusque.so` داخل APK نهایی.
- اضافه شدن manifest هش هسته‌ها و توقف کامل انتشار در صورت حذف هر هسته.
