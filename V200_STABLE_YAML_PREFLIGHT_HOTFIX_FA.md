# اصلاح اعتبارسنجی YAML در انتشار پایدار 2.0.0

خطای `ModuleNotFoundError: No module named 'yaml'` به این دلیل رخ می‌داد که
اسکریپت انتشار، وجود PyYAML را روی Python کاربر فرض کرده بود.

در این Hotfix:

- نسخه pure-Python از PyYAML 6.0.3 همراه سورس بسته‌بندی شده است.
- اعتبارسنجی Workflowها دیگر به نصب سراسری، pip یا اینترنت وابسته نیست.
- Validator جدید همه فایل‌های `.yml` و `.yaml` را بررسی می‌کند.
- کلید تکراری، Tab، فایل خالی و Workflow بدون `jobs` باعث توقف قبل از Push می‌شود.
- مجوز MIT و اطلاعات منبع PyYAML داخل بسته درج شده است.
