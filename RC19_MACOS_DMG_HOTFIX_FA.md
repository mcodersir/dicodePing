# هات‌فیکس ساخت DMG مک در RC19

خطای GitHub Actions فقط در runner مک Intel و در آخرین مرحله ساخت DMG رخ می‌داد:

```text
hdiutil: create failed - Resource busy
```

خود برنامه و bundle مک با موفقیت ساخته شده بودند. این هات‌فیکس مرحله DiskImages را پایدار می‌کند:

- هر تلاش در پوشه staging کاملاً جدید انجام می‌شود.
- خروجی هر تلاش نام موقت و مستقل دارد.
- خطاهای گذرای `EBUSY`، `EAGAIN`، timeout و unmount تا شش بار با backoff تکرار می‌شوند.
- فایل DMG قبل از انتقال به پوشه release با `hdiutil verify` بررسی می‌شود.
- فقط فایل تأییدشده به نام نهایی منتقل می‌شود؛ خروجی ناقص منتشر نمی‌شود.
- Workflow در سطح کل build مک نیز سه تلاش مستقل دارد.
- پیش از Upload، وجود فایل و صحت DMG دوباره بررسی می‌شود.

این تغییر فقط بسته‌بندی macOS را اصلاح می‌کند و منطق TUN، پینگ دوگانه TCP/Xray و پایداری اتصال RC19 بدون تغییر باقی مانده است.
