# هات‌فیکس انتشار dicodePing 2.0.0 RC1

این بسته توقف انتشار پس از Push موفق شاخه main و قطع موقت اتصال HTTPS به GitHub را رفع می‌کند.

- Push شاخه main تا ۸ مرتبه تکرار می‌شود.
- وجود Commit روی GitHub قبل از Tag بررسی می‌شود.
- Tag با Git References API ساخته و SHA آن کنترل می‌شود.
- Workflow انتشار به‌صورت صریح روی همان Tag اجرا می‌شود.
- تمام درخواست‌های GitHub CLI با Backoff و بازیابی احراز هویت تکرار می‌شوند.
- اجرای دوباره اسکریپت امن و idempotent است؛ Release و Tag قبلی به شکل کنترل‌شده جایگزین می‌شوند.
