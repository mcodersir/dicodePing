# گزارش اصلاحات v1.9.0-rc.14

## اتصال واقعی هسته‌های Android

- انتخاب Aether یا WARP دیگر صرفاً یک گزینه ظاهری نیست؛ دکمه اتصال صفحه اصلی همان هسته انتخاب‌شده را اجرا می‌کند.
- هدف اتصال خارجی با شناسه مستقل `core:aether` یا `core:warp` ساخته می‌شود و کارت سرور Xray به‌اشتباه نمایش داده نمی‌شود.
- مسیر واقعی ترافیک: `Android VpnService/TUN → Xray SOCKS bridge → Aether یا Usque SOCKS → اینترنت`.
- پردازش native هسته با محیط HOME/TMP/XDG و مسیر کتابخانه‌های APK اجرا می‌شود.
- آماده‌شدن پورت SOCKS به‌تنهایی کافی نیست؛ یک درخواست HTTP واقعی باید از تونل عبور کند.
- Aether و WARP ابتدا مسیر اصلی را اجرا می‌کنند و در صورت شکست به HTTP/2 fallback می‌روند.
- زمان‌های راه‌اندازی برای اسکن Aether، ثبت WARP و اعتبارسنجی واقعی جدا شده‌اند.
- لغو اتصال، شکست تلاش اول و خروج سرویس، پردازش native و TUN را پاک‌سازی می‌کند.

## تعامل کاربر

- پس از فعال‌کردن Aether یا WARP، دیالوگ دقیق نمایش داده می‌شود: رفتن به خانه، زدن دکمه اتصال و صبر تا وضعیت «متصل».
- دکمه «رفتن به صفحه اصلی» مستقیماً کاربر را به Home می‌برد.
- ثبت اولیه WARP از تنظیمات حذف و به جریان foreground اتصال منتقل شد تا پیشرفت و خطا قابل مشاهده باشد.
- مراحل آماده‌سازی، ثبت WARP، اجرای مسیر اصلی، fallback، آماده‌شدن SOCKS و تست ترافیک در وضعیت اتصال نشان داده می‌شوند.

## Android UI و اسکنر

- یکپارچه‌سازی «برنامه‌های خارج از اتصال» با denylist برنامه‌محور و مهاجرت خودکار داده قدیمی.
- حذف floating hintهای هم‌پوشان در Dropdownهای زبان، پوسته، هسته و مسیریابی.
- اتصال خطای اعتبارسنجی CDN به فیلد صحیح.
- تفکیک badgeهای کیفیت، نوع کانفیگ و حجم در کارت سرور.
- progress مستقل برای دریافت کانفیگ و آزمایش سرورها.
- شمارنده تست/سالم و لاگ زنده تا ۲۴۰ خط با highlight بهتر.

## Repository / CI

- workflow انتشار، Aether و Usque واقعی را برای ABIهای ۶۴ بیتی آماده و حضور ELFها را داخل APK کنترل می‌کند.
- حذف workflowهای انتشار قدیمی، اسکریپت‌های RC و فایل‌های validation نسخه‌ای.
- نگه‌داری فقط workflowهای CI، CodeQL، Pages و Release.
- ترمیم GitHub Pages با build/deploy مجزا.

## اعتبارسنجی انجام‌شده

- ۹۳ تست Python
- Python compileall
- Android XML/string/project validation با ۲۶۵ رشته محلی‌سازی‌شده
- کامپایل مستقل command builder با Kotlin compiler
- Gradle Kotlin DSL static validation
- security quality gate

بیلد نهایی APK و تست روی گوشی در این محیط اجرا نشد، چون دریافت Gradle/Android SDK/NDK و باینری‌های وابسته به دسترسی شبکه و محیط Android نیاز دارد. workflow انتشار برای ساخت و بررسی APK کامل شده است.
