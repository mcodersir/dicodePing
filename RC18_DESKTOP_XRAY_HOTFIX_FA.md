# اصلاح قطعی TUN دسکتاپ در RC18

RC18 اصلاح‌شده دیگر از System Proxy به‌عنوان مسیر اتصال Xray استفاده نمی‌کند. مسیر اصلی روی Windows، Linux و macOS به‌صورت کامل چنین است:

`ترافیک سیستم‌عامل → Xray TUN inbound → سرور انتخاب‌شده → اینترنت`

اصلاحات انجام‌شده:

- ورودی اصلی Xray روی هر سه سیستم‌عامل از نوع `tun` است.
- جدول مسیریابی پیش‌فرض IPv4 و IPv6 از طریق `autoSystemRoutingTable` به TUN منتقل می‌شود.
- برای جلوگیری از حلقه اتصال، Xray از `autoOutboundsInterface=auto`، `sendThrough` و Host Route مستقیم برای IP سرور استفاده می‌کند.
- Windows همراه `wintun.dll` بسته‌بندی می‌شود و نسخه نهایی با UAC اجرا می‌گردد.
- Linux هنگام شروع از طریق PolicyKit دسترسی لازم برای `CAP_NET_ADMIN` را می‌گیرد و محیط کاربر حفظ می‌شود.
- macOS از نام معتبر `utunN` استفاده می‌کند و مجوز مدیر را با پنجره رسمی سیستم درخواست می‌کند.
- قبل از اعلام «متصل»، ابتدا خود سرور از یک SOCKS خصوصی داخل همان Xray بررسی می‌شود؛ سپس یک درخواست مستقیم بدون System Proxy اجرا می‌شود تا فعال‌شدن واقعی TUN سراسری تأیید شود.
- اگر سرور کار کند ولی Route سیستم فعال نشود، برنامه وضعیت متصل نمایش نمی‌دهد و خطای مشخص TUN نشان می‌دهد.
- هنگام قطع اتصال، پردازش Xray، Routeهای مستقیم موقت، فایل Runtime و رابط TUN پاک می‌شوند.
- Snapshot قدیمی System Proxy فقط برای بازیابی کاربرانی نگه داشته شده که نسخه ناقص قبلی RC18 را اجرا کرده‌اند؛ RC18 اصلاح‌شده هیچ System Proxy جدیدی فعال نمی‌کند.

## دسترسی لازم

برنامه را عادی اجرا کنید. نسخه Windows پنجره UAC، نسخه Linux پنجره PolicyKit و نسخه macOS پنجره رمز مدیر را به‌صورت خودکار باز می‌کند. لغو این مجوز به معنی لغو اتصال TUN است.
