# اصلاح دیپلوی RC18

این Hotfix خطای باقی‌ماندن تست‌های نسخه‌های قبلی پس از Extract روی پوشه قدیمی را رفع می‌کند.

- فایل‌های `test_rc10_*.py` تا نسخه قبل از RC جاری، بدون وابستگی به متن داخل فایل حذف می‌شوند.
- تست‌های عمومی قدیمی مانند `test_rc2.py` تا `test_rc9.py` حفظ می‌شوند.
- پس از Cleanup یک بررسی مستقل با `--check` انجام می‌شود و فقط در صورت پاک‌بودن Snapshot، Push ادامه پیدا می‌کند.
