# هات‌فیکس Runtime اتصال TUN در RC18

## علت خطا

لاگ نشان می‌داد رابط Wintun ساخته شده و ترافیک DNS، تلگرام و HTTPS از `tun-in` به `proxy` وارد می‌شود، اما تست خصوصی برنامه فقط اتصال HTTP روی پورت 80 را می‌پذیرفت. در بعضی سرورها پورت 80 یا پاسخ همان سایت‌های تست محدود است؛ بنابراین برنامه یک TUN در حال کار را اشتباهاً متوقف می‌کرد.

هم‌زمان مسیر خطای رابط کاربری، متغیر تعریف‌نشده `old` را در `mark_probe_failed` استفاده می‌کرد و پس از خطای اتصال با `NameError` بسته می‌شد.

## اصلاح

- تست SOCKS با HTTPS روی پورت 443 شروع می‌شود و HTTP فقط fallback است.
- پاسخ‌های تکه‌تکه SOCKS و HTTP با دریافت دقیق و status-line کامل خوانده می‌شوند.
- نتیجه SOCKS کمکی است؛ معیار نهایی یک درخواست عادی از مسیر سراسری TUN است.
- مسیرهای `mark_probe_failed`، `toggle_favorite` و refresh دیگر به متغیر تعریف‌نشده دسترسی ندارند.
- Validator مستقل قبل از Push و در GitHub Actions این اصلاحات را کنترل می‌کند.
