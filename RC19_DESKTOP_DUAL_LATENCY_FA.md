# RC19 — تست واقعی TCP و Xray در دسکتاپ

در این نسخه هر سرور دو بار با دو مسیر متفاوت سنجیده می‌شود:

1. **TCP**: فقط یک handshake مستقیم به IP/Host و Port سرور برای نمایش دسترسی شبکه.
2. **Xray**: یک درخواست HTTP واقعی از داخل Outbound همان کانفیگ برای سنجش کارکرد واقعی پروتکل، TLS/Reality و Transport.

TCP هیچ‌وقت به‌تنهایی سرور را سالم نمی‌کند. وضعیت آنلاین و انتخاب خودکار فقط از نتیجه Xray ساخته می‌شود. هر دو مقدار جداگانه در فایل سرورها، تاریخچه اسکنر و رابط کاربری ذخیره و نمایش داده می‌شوند.
