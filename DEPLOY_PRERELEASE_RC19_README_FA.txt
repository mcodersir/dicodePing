dicodePing v1.9.0 RC19 - Real TCP + Xray Desktop Scanner

روش اجرا:
1) ZIP را در یک پوشه کاملاً جدید Extract کنید.
2) فایل DEPLOY_PRERELEASE_RC19.bat را اجرا کنید.
3) اسکریپت Clone، پاک‌سازی، اعتبارسنجی، Push، ساخت Tag، Pre-release و GitHub Pages را انجام می‌دهد.

تغییر اصلی RC19:
- برای هر کانفیگ دسکتاپ دقیقاً یک تست TCP به Host/Port انجام می‌شود.
- سپس دقیقاً یک تست HTTP واقعی از داخل Xray همان کانفیگ اجرا می‌شود.
- TCP فقط دسترسی شبکه را نشان می‌دهد و هرگز به‌تنهایی سرور را سالم نمی‌کند.
- وضعیت آنلاین و انتخاب خودکار فقط بر اساس نتیجه Xray است.
- TCP و Xray در دو ستون و دو badge رنگی جدا نمایش داده و دائمی ذخیره می‌شوند.

Android signing secrets در صورت نبود توسط اسکریپت ساخته یا بازیابی می‌شوند.
کلید خصوصی دائمی را از Documents\dicodePing-signing حذف نکنید.
