name: v0.1.3 Release Validation

on:
  pull_request:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: write

jobs:
  validate-entrypoint:
    name: Validate release workflow entrypoint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "v0.1.3 release workflow is active"
